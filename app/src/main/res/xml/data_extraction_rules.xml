<?xml version="1.0" encoding="utf-8"?>
<!--
  Data extraction rules. allowBackup is false in the manifest, so this
  file's role is to make the no-backup posture explicit on Android 12+.
  The app stores only local-only data; we do not ship it off-device,
  including via cloud backup or device-to-device transfer.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="file" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="external" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="file" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="external" />
    </device-transfer>
</data-extraction-rules>
